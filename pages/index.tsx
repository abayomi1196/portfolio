import type { NextPage } from 'next';
import 'twin.macro';

import Layout from 'sections/Layout';

const Home: NextPage = () => {
  return (
    <Layout>
      <h1>Main Page</h1>
    </Layout>
  );
};

export default Home;
